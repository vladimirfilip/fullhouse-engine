# Libraries available inside the bot sandbox container.
# Listed here for reference — install them locally with `make install`
# (eval7 needs Cython<3 + --no-build-isolation, so a plain
# `pip install -r requirements.txt` will fail on eval7).
eval7==0.1.7
flask>=3.0.0
numpy>=1.26.0
scipy>=1.13.0
treys==0.1.8
scikit-learn>=1.5.0
